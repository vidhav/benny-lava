name: Publish
on:
  push:
    tags:
      - "v*"
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set version
        run: |
          sed -i "s/0.0.0/${GITHUB_REF:11}/" ./benny_lava/__init__.py
